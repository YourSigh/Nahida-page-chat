{
  "name": "nahida-page-chat",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "sharp": "^0.33.5"
  },
  "dependencies": {
    "markdown-it": "^14.1.1"
  }
}
