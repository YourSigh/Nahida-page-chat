{
  "name": "nahida-page-chat",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/content/index.js --bundle --format=iife --target=chrome109 --outfile=content.js --loader:.css=text",
    "dev": "esbuild src/content/index.js --bundle --format=iife --target=chrome109 --outfile=content.js --loader:.css=text --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}

